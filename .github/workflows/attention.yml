name: Englishman

on:
  push:

jobs:
  neck:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        design: ["fortune", "odd", "considerable", "alternative", "ribbon"]
        heating: ["saint", "physics", "angry", "crime"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar fw.jar
